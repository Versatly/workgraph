/**
 * Cursor Cloud adapter placeholder for dispatch contract integration.
 *
 * MVP behavior: local compatibility shape only; network transport can be
 * layered without changing this package interface.
 */

export function adapterName(): string {
  return 'cursor-cloud';
}
